apiVersion: v1
kind: Service
metadata:
  name: fish-tts
  namespace: private-reading
spec:
  type: ClusterIP
  selector:
    app: fish-speech
  ports:
  - name: http
    port: 8013
    targetPort: 8080
    protocol: TCP
