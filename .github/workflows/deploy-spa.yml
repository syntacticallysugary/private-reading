name: Deploy SPA
on:
  push:
    branches: [ main ]
    paths:
      - 'spa/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.ARM_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H 64.181.220.163 >> ~/.ssh/known_hosts

      - name: Inject config
        run: |
          sed -i \
            -e "s|<YOUR_OCI_API_GATEWAY_URL>|${{ secrets.API_BASE_URL }}|g" \
            -e "s|<YOUR_COGNITO_APP_CLIENT_ID>|${{ secrets.COGNITO_CLIENT_ID }}|g" \
            -e "s|<YOUR_AWS_REGION>|us-east-1|g" \
            spa/app.js

      - name: Deploy SPA
        run: |
          rsync -az --delete \
            -e "ssh -i ~/.ssh/deploy_key" \
            spa/ ubuntu@64.181.220.163:/var/www/reading.syntacticallysugary.dev/

      - name: Verify
        run: |
          ssh -i ~/.ssh/deploy_key ubuntu@64.181.220.163 \
            "ls /var/www/reading.syntacticallysugary.dev/"
